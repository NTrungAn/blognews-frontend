# ─────────────── Cấu hình Môi trường Frontend (Mẫu) ───────────────
# Nhân bản (duplicate) file này thành ".env.local" và điền các giá trị phù hợp.

# URL gốc của Spring Boot backend API
VITE_API_BASE_URL=http://localhost:8080/api
